Для общения с разными приложениями, можно использовать концепцию io комнат.

Pub-Sub
// main.js
const { fork } = require('child_process');
fork('slave');
process.send('main / hello');
process.on('message', (message) => {
  console.log('main / message', message);
});

// slave.js
process.on('message', (message) => {
  console.log('test / message', message);
  process.send('test / hi');
});
